(ns gov.stockport.sonar.ingest.faking.events.yos
    (:require [gov.stockport.sonar.ingest.faking.helpers :as h]
      [clj-time.core :as t]
      [gov.stockport.sonar.ingest.faking.config :as cfg]
      [gov.stockport.sonar.ingest.faking.helpers :refer [make]]))


(defn non-statutory-interventiosns
      "Generates fake Non-Statutory-Interventions"
      []


      )

(defn statutory-intervention
       "Generates statutory interventions"[]



      )