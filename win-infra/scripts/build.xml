<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <Target Name="nsis-setup">
      <Exec Command="C:\mapped-from-host\files\nsis-setup.exe /S"/>
  </Target>

  <PropertyGroup>
          <ChocLocation>c:\ProgramData\chocolatey</ChocLocation>
  </PropertyGroup>
  <Target Name="install-choco" Condition="!Exists('c:\ProgramData\chocolatey')">
      <Exec Command="powershell C:\mapped-from-host\scripts\install-choco.ps1"/>
  </Target>

  <Target Name="install-build-deps" DependsOnTargets="install-choco">
      <Exec Command="powershell C:\mapped-from-host\scripts\install-build-deps.ps1"/>
  </Target>

  <Target Name="something-else">
   <Exec Command="&quot;C:\Program Files (x86)\NSIS\makensis&quot; ^
       C:\mapped-from-host\service-stuff\installer.nsi"/>
   <Exec Command="C:\mapped-from-host\SignpostsVisualiserSetup.exe /S"/>
       <Message Text="bizzle"/>
  </Target>

  <Target DependsOnTargets="install-build-deps;something-else" Name="run">
  </Target>

</Project>
